#include <stdio.h>
#include <stdlib.h>
#include "madfortshell.h"

/**
  *total_value - a function that finds total value of two numbers
  *@x: one value
  *@y: the other value
  *Return: Total
  */
int total_value(int x, int y)
{
	int total;

	total = x + y;
	return (total);
}
