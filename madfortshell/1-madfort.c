#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
  *total_value - finds the total value of two numbers
  *@x: first value
  *@y: second value
  *Return: the total value of two numbers
  */
int total_value(int x, int y)
{
	return (x + y);
}
