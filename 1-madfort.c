#include <stdio.h>
#include <stdlib.h>
#include "shell.h"

/**
  *total_value - finds the total value of two numbers
  *@x: first number
  *@y: second number
  *Return: the value of the two numbers
  */
int total_value(int x, int y)
{
	return (x + y);
}
